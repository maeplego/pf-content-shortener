# Kubernetes マニフェスト（P08 shortener）

短縮とクリック計測です。このフォルダだけを apply しないでください。起動は [pf-cloud-k8s](https://github.com/maeplego/pf-cloud-k8s) の content overlay からです。`shortener.localhost` です。許可ホストは `blog.localhost` です。
